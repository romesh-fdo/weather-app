version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build process..."
        - cp config.example.js config.js
    build:
      commands:
        - echo "Building the app..."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []